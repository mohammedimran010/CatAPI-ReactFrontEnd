import Home from "./components/Home";
import Upload from "./components/Upload";

const navLinks = [
    { text: 'Home', path: '/', component: Home},
    { text: 'Upload', path: '/upload', component: Upload},
];

export default navLinks;

